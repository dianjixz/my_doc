#licheepi nano体验报告
主要主要体验的过程
先在网上找了一些教程，然后和官网上手册作一些对nano进行linux编译运行。

在运行过程中遇到了一些问题。
第一，在spi-flash 启动适配的时候修改修改 suniv.h 文件的所填写的指令有错误。
    sf probe 0:50000000; 这个命令有错误。正确命令应该是sf probe 0 50000000；

第二，在dts 修改中，最新的分支已经修改过了，不需要在修改了。其次，修改文件中的flash型号错误，
    因该是xt25f128b。

第三，这次的编译虽然是对的，但是nano的buildroot编译不能使用glibc，不能使用本机交叉编译工具链
    ，否册将会报错，主要报非法指令，目前还没有定位到具体的问题。

在运行时，添加了自己的hellowrd工程，在buildroot工程下编译使用正常。